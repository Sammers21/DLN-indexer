apiVersion: v1
kind: Service
metadata:
  name: {{ include "dln-api.fullname" . }}
  labels:
    {{- include "dln-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "dln-api.selectorLabels" . | nindent 4 }}
