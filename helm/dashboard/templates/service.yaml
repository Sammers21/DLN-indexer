apiVersion: v1
kind: Service
metadata:
  name: {{ include "dln-dashboard.fullname" . }}
  labels:
    {{- include "dln-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "dln-dashboard.selectorLabels" . | nindent 4 }}
